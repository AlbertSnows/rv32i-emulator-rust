hardwarde modes


what are the modes?
- S
- M
- U
- what else?

implementation steps
- add mode to cpu state
- add mode reader, mode writer

default state: m mode 

move state 
- questions
-- how does the cpu move state? what are all the ways it can move state?
--- MPP = machine previous Privilege
---- what is mpp? 
- 2 its, instide mstatus (what is mstatus?)
- parallel to mepc
- mpp remembers what the cpu was in (m, s, u) when trap happens


what is mstatus? 
- a csr 
- same as mepc, mcause, etc. 
- mstatus is a single 32bit slot 
- carved into many named fields 
-- MIE 
-- MPIE 
-- MPP 
- writing to MPP is messing wit bits 11-12?
- 

---- lives in mstatus? 
--- two means to update mode
---- trap entry
- save the current mode into MPP
- set the current mode to m

---- trap return 
- mret 
-- what is mret?
- op code 
- same as ecall/ebreak
- sets pc from mepc
- sets privilege mode to mpp 

----- handle trap 
- save mode to mpp. 
- switch to m 

----- return from trap 
- mret 
- restores the mode from mpp 
- restores pc from mepc

-- when it does move state, does anything change except the cpu state?
- no, just state 

-- what are the effects of moving state? 
- csr access control 
-- reading/writing from csr below required minimum privledge should raise IllegalInstruction
- mret legality
-- xret instruction can be executed in privlege mode x or higher 
-- if below x, illegal instruction exception
-- mret needs a mode check before it's allowed to run 


notes from the book: 
> To support nested traps, 
each privilege mode x that can respond to interrupts has a two-level stack of interrupt-enable bits and privilege modes. 
xPIE holds the value of the interrupt-enable bit active prior to the trap, 
and xPP holds the previous privilege mode. 
The xPP fields can only hold privilege modes up to x,
so MPP is two bits wide and SPP is one bit wide. 

When a trap is taken from privilege mode y into privilege mode x, 
xPIE is set to the value of xIE; 
xIE is set to 0; 
and xPP is set to y

xIE = x interupt enabled. 
this is a bit, it answers this question:
"is this privilege level currenly willing to be interrupted?"

xPIE = previous interrupt enable 
- MPIE for now 
- saves a record of xIE before xIE is changed 
- meant to be a restore point for xIE 

xPP = x previous privilege  maps onto MPP for us.


--- remaining steps (one at a time) ---

verified facts, ready to use:
- MSTATUS csr address = 0x300 (Table 7, p.20)
- mstatus bit positions (RV32): MIE=bit 3, MPIE=bit 7, SPP=bit 8, MPP=bits 11-12
- funct12 values (SYSTEM opcode, funct3=000): ECALL=0x000, EBREAK=0x001, MRET=0x302
  (SRET=0x102, WFI=0x105 -- not needed yet, no S-mode work planned)

[ ] 0. fix current compile errors in i/system.rs before adding anything new:
      - CPUState isn't imported (only RegisterFile is)
      - execute_i_system_type's first match compares `mode` (a CPUMode) against
        TrapCause variants -- wrong type, doesn't compile
      - CPUMode needs #[derive(..., PartialEq, Copy, Clone)] added in
        cpu_definition.rs (currently only Debug) -- needed for `let mode = cpu.mode`
        and any test assertions

[ ] 1. add MSTATUS constant to codes.rs, same pattern as MTVEC/MEPC/MCAUSE/MTVAL

[ ] 2. fix parse_system_inst's discrimination logic (i/system.rs):
      replace the BIT_TWENTY-only check with a full 12-bit match on
      masks::CSR_ADDRESS (funct12), using the values above. add SystemOp::MRet
      as a real, reachable parse result (not just a dead enum variant).

[ ] 3. extend handle_trap (core.rs): before jump_to_trap_handler, save the
      current mode into mstatus's MPP bits (11-12), then set cpu.mode = M.
      needs read-modify-write on mstatus (read current value, clear bits 11-12,
      OR in the new mode bits, write back) -- same shape as mask_and_shift/
      merge_bits used all over decoding, just applied to a CSR value.

[ ] 4. implement SystemOp::MRet execution: check cpu.mode == M first (else
      IllegalInstruction), then set pc from mepc and mode from mstatus's MPP
      bits -- mirrors jump_to_trap_handler's read-then-write pattern.

[ ] 5. ecall's mcause should depend on cpu.mode, not be hardcoded to
      EnvironmentCallFromMMode. needs new TrapCause variants
      (EnvironmentCallFromSMode/UMode) and execute_i_system_type picking
      based on cpu.mode.

[ ] 6. CSR privilege-gating: CsrState::write (and read) needs to compare
      cpu.mode against the CSR address's required-privilege bits (address[9:8]),
      same idea as the existing read-only check on address[11:10]. requires
      threading mode into every csr.write/read call site.

[ ] 7. tests for each of the above.

