enums by format

little endian: right -> left
e.g. for 0x002081B3, 0x1000 is B3

recipe has instructions
pantry has numbers (memory)

components:
- fetch
- decode
- execute

--- remaining work ---

up next:
[ ] x0-always-zero enforcement (RegisterFile is still a plain array, no special-casing)
[ ] SYSTEM instructions -- start with ebreak, so the loop has a real halt signal
    instead of overloading fetch-out-of-bounds as the stopping condition
    (ecall, csrrw/csrrs/csrrc/csrrwi/csrrsi/csrrci after that)

toward a working loop:
[ ] pc advancement after execute (pc += 4 -- outer loop's job, per earlier decision)
[ ] the actual fetch-decode-execute loop (currently runs once, linearly, no loop at all)
[ ] loop stopping condition -- ebreak, once implemented

instruction coverage:
[ ] execute logic for the other 9 R-type ops (sub, sll, slt, sltu, xor, srl, sra, or, and)
    -- AluOp already covers all 10, only inst_r_add exists
[ ] parsers for the other 5 formats (I/S/B/U/J) -- still unit-variant placeholders

deferred:
[ ] tests for the other 8 R-type ops

