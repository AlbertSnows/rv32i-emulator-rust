enums by format

little endian: right -> left
e.g. for 0x002081B3, 0x1000 is B3

recipe has instructions
pantry has numbers (memory)

components:
- fetch
- decode
- execute

--- remaining work ---


instruction coverage (execution side):
[ ] execute logic for the other 9 R-type ops (sub, sll, slt, sltu, xor, srl, sra, or, and)
    -- AluOp match now has an explicit arm per variant (todo!() stubs), return
    type settled as Result<ExecutionSignal, String> on execute_r_type
[ ] execute logic for B/J/S/U/JALR/AluImm/Shift/Load -- all still
    Ok(ExecutionSignal::Continue) stubs, none touch registers/memory yet
    (system.rs is the only execute_* that's real so far)

[ ] load.rs: switch lb/lh/lw/lbu/lhu address calc from plain `+` to
    wrapping_add (val.wrapping_add(imm_i as u32) as usize), matching the
    s.rs store-address fix -- same overflow/panic risk, not yet applied

deferred:
[ ] remaining SYSTEM instructions (csrrw/csrrs/csrrc/csrrwi/csrrsi/csrrci) -- later

